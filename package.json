{
  "name": "only-error-relief-coordinator",
  "version": "1.0.0",
  "description": "PS20 — Disaster Relief & Emergency Resource Coordinator: backend agents + mobile-responsive client, per master architecture v2",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test tests/"
  },
  "engines": { "node": ">=18" },
  "dependencies": {
    "express": "^4.19.2"
  }
}
